name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: pip install -e ".[dev,ui]"
      - name: UI import smoke test
        run: python -c "import tabito_itemgen.ui_app; import tabito_itemgen.ui_launcher"
      - name: Test
        run: pytest -q
